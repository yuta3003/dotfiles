#!/bin/sh

GRML_OSTYPE=$(uname -s)

if [ $GRML_OSTYPE = "Darwin" ]; then
  ifconfig en0 inet | sed -e '1,2d' -e 's/^.*inet //' -e 's/ netmask.*$//'
elif [ $GRML_OSTYPE = "Linux" ]; then
  ifconfig wlp2s0 | grep -E "inet " | sed -e 's/^.*inet //' -e 's/ netmask.*$//'
fi

